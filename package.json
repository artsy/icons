{
  "name": "@artsy/icons",
  "version": "0.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf build/svg && rm -rf build/src",
    "optimize": "svgo src -o build/svg --config=svgo.config.js",
    "build": "yarn clean && yarn optimize && node scripts/build.js"
  },
  "devDependencies": {
    "@svgr/core": "^6.3.1",
    "fs-extra": "^10.1.0",
    "glob": "^8.0.3",
    "lodash": "^4.17.21",
    "svg-parser": "^2.0.4",
    "svgo": "^2.8.0",
    "typescript": "^4.8.3"
  }
}
